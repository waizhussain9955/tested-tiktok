# Environment
APP_NAME=TikTok Downloader API
APP_VERSION=1.0.0
ENVIRONMENT=development

# Server
HOST=0.0.0.0
PORT=8000

# Cache
CACHE_TTL=3600
CACHE_TYPE=memory

# API
API_PREFIX=/api/v1

# Rate Limiting (future)
RATE_LIMIT_PER_MINUTE=30

# Redis (when ready for production)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_DB=0
